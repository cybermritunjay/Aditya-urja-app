
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    error: {
      color: 'green',
      marginBottom: 20,
    },
    errorContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 20,
    },
  })

  export default styles;
